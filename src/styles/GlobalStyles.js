import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  textBold: {
    fontWeight: 'bold',
  },
  containerPadding: {
    padding: 16,
  },
  sectionMargin: {
    marginVertical: 15,
  },
});

export default GlobalStyles;
